customization:
  distribution_code_name: Weasel
  distribution_version: 0.14.3
  generator: "Rime::SwitcherSettings"
  modified_time: "Fri Oct 23 03:01:08 2020"
  rime_version: 1.5.3
patch:
  key_binder:
    bindings:
      - { when: composing, accept: Shift+Tab, send: Shift+Left }
      - { when: composing, accept: Tab, send: Shift+Right }
  menu:
    page_size: 10
  schema_list:
    - {schema: combo_pinyin_stroke}
    - {schema: combo_pinyin_lrow}
    - {schema: combo_pinyin_rrow}
    - {schema: sydx}
